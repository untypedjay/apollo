import React from 'react';

interface Props {
  show: any
};

function ShowCard({ show }: Props) {
  return (
    <div className="show-card">
      ShowCard
    </div>
  );
}

export default ShowCard;